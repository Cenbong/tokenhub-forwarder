name: tokenhub-forwarder
region: fra
type: web
env: docker
docker:
  dockerfile: Dockerfile
  context: .
ports:
  - port: 3000
    protocol: http
health:
  path: /health
  initial_delay: 5
  period: 30
scaling:
  min: 0
  max: 1
routes:
  - path: /
    port: 3000